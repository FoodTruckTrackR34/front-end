// Name
// username
// password
// confirmpassword:
// Location:        (optional) (placeholder text: enter zip code, address, GPS coordinates, or leave blank for now! )

export default function RegisterForm () {
    return (
        <div>this is a test</div>
    );
}